# 🌐 01. WEB

<details>
<summary>1. Web이란?</summary> </br>

웹(Web)은 클라이언트와 서버가 HTTP를 통해 서로 통신하는 서비스 구조이다.</br>

사용자가 브라우저에서 특정 URL에 접근하면, 클라이언트는 서버로 HTTP `요청(Request)`을 보낸다.</br>

서버는 해당 요청을 분석한 후 결과 데이터를 HTTP `응답(Response)` 형태로 다시 반환한다.</br>

백엔드는 이 요청–응답 흐름 속에서 바로 `서버 영역`을 담당한다.</br>

이를 순서상으로 정리하자면 아래와 같다. </br>
사용자가 URL에 접근 -> 클라이언트가 서버로 HTTP 요청을 보냄 -> 서버는 요청 분석 후 결과를 HTTP 응답 데이터로 반환

즉, 백엔드는 단순히 데이터를 저장하는 곳이 아니라
요청을 처리하고 결과를 만들어내는 서버 영역이다.

</details>

<details>
<summary>2. WEB의 주요 구성 요소 </summary> </br>

- 클라이언트(Client) </br>

클라이언트는 사용자가 웹 내 정보를 탐색할 수 있게 도와주는 역할을 한다. </br>

우리 주변에서 쉽게 볼 수 있는 예시로는 Google Chrome, Safari 등이 있다. </br>

클라이언트는 `웹브라우저` 로도 불린다. </br>

즉, 우리가 웹페이지에 접근할 수 있게 도와주는 소프트웨어라고 생각하면 된다. </br>

브라우저는 사용자가 입력한 URL을 통해 웹 페이지를 요청하고, 서버에서 받은 데이터를 해석해 화면에 표시한다. </br>

- 서버(Server) </br>

웹 서버는 클라이언트의 요청을 처리하고 필요한 데이터를 응답으로 반환하는 역할을 한다. </br>

쉽게 말하자면 사용자에게 필요한 정보(웹사이트, 데이터 등)를 제공(Serve)해주는 역할이다. </br>

대표적인 웹 서버에는 Apache, Nginx 등이 있다. </br>

- 웹 프로토콜 </br>

웹 프로토콜이란 클라이언트와 서버가 데이터를 주고받는 통신 규칙을 말한다. </br>

웹이 원활하게 작동하기 위해선 여러 프로토콜이 필요한데 이러한 프로토콜은 데이터 전송의 규칙을 정의하는 역할을 한다.</br>

1) HTTP </br>

   (1) HTTP의 정의 </br>
   
   클라이언트와 서버 간의 데이터를 주고 받기 위한 프로토콜이다. </br>

   (2) HTTP의 특징 </br>
   
   1️⃣ 요청 - 응답 구조 </br>
   
   보통 클라이언트에 의헤 전송되는 메세지를 요청(Request)라고 부르며 서버에서 응답하는 메세지를 응답(Response)라고 부른다. <br>

   클라이언트 → Request → 서버 → Response → 클라이언트 순으로 진행된다.

   2️⃣ 무상태 </br>
   
   HTTP는 이전의 상태를 기억하지 않는다. </br>

   따라서 쿠키 또는 세션을 이용해 정보를 저장해야한다. </br>

   (3) HTTP 메서드 </br>
   HTTP 메서드에는 GET, POST, PUT, DELETE, PATCH 등이 있다. </br>

   - GET </br>

   사용자가 웹 페이지에 접근할 때 GET 요청이 발생한다. </br>

   GET은 서버로부터 어떠한 데이터를 가져오기 위해 사용된다. </br>

   여기서 중요한 점은,
   `값이나 내용, 상태 등을 바꾸지 않고` 사용자에게 단지 보여주기 위한 용도일때만 사용된다. </br>

   - POST </br>

   POST 요청은 사용자가 웹 페이지에서 데이터를 제출할 때 사용된다. </br>

   POST 요청은 보통 HTML 양식을 통해 서버에 전송되며, GET 방식과 달리 서버상의 `데이터 값이나 내용 등을 바꾸기위해` 사용된다. </br>

   사용자가 웹 페이지 내에서 정보를 입력 한 후, 데이터를 제출하면 이 정보는 POST 방식을 통해 서버에 전달되는 것이다. </br>
   

  
   
     

   



</details> 
